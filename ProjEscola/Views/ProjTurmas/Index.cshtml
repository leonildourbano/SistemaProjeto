@model IEnumerable<ProjEscola.Models.ProjTurma>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTurma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataInicioTurma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataTerminoTurma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoTurma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioTurma)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTurma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataInicioTurma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataTerminoTurma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoTurma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioTurma)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
