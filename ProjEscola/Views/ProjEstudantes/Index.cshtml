@model IEnumerable<ProjEscola.Models.ProjEstudante>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeEstudante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailEstudante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FoneEstudante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascimentoEstudante)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeEstudante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailEstudante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoneEstudante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataNascimentoEstudante)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
